---
phase: 12
plan: 12-04
title: "CLI Commands & Unit Tests for ARP and Composite Tools"
wave: 2
depends_on: [12-01, 12-02, 12-03]
files_modified:
  - nautobot_mcp/cli/cms_interfaces.py
  - nautobot_mcp/cli/cms_routing.py
  - nautobot_mcp/cli/cms_firewalls.py
  - tests/test_cms_arp.py [NEW]
  - tests/test_cms_composites.py [NEW]
autonomous: true
requirements_addressed: [ARP-01, ARP-02, COMP-01, COMP-02, COMP-03, COMP-04]
---

<objective>
Add CLI commands for ARP entries (under the existing interfaces CLI) and composite summary commands (per-domain with `-summary` suffix). Create comprehensive unit tests for the ARP model, CRUD functions, and all 4 composite summary functions.
</objective>

<must_haves>
- ARP CLI commands under `nautobot-mcp cms interfaces` (list-arp-entries, get-arp-entry)
- Routing composite CLI commands: `bgp-summary`, `routing-table` under `nautobot-mcp cms routing`
- Interface composite CLI command: `detail` under `nautobot-mcp cms interfaces`
- Firewall composite CLI command: `firewall-summary` under `nautobot-mcp cms firewalls`
- All composite CLI commands support `--detail` flag
- Unit tests for ARP model and CRUD
- Unit tests for all 4 composite functions
- All existing tests continue to pass
</must_haves>

---

## Task 1: Add ARP CLI Commands to cms_interfaces.py

<read_first>
- nautobot_mcp/cli/cms_interfaces.py (existing interface CLI commands — Typer app, table formatting pattern)
- nautobot_mcp/cli/cms_routing.py (another CLI pattern reference — --detail flag usage)
- nautobot_mcp/cms/arp.py (list_arp_entries, get_arp_entry functions)
</read_first>

<action>
Add 2 ARP CLI commands to `nautobot_mcp/cli/cms_interfaces.py` (since ARP goes under interfaces per CONTEXT.md):

**1. `list-arp-entries` command**
Options: `--device` (required), `--interface` (optional), `--mac-address` (optional), `--limit` (default 0)
Table columns: MAC Address | IP Address | Interface | Hostname | Device
Calls: `arp.list_arp_entries(client, device=device, interface=interface, mac_address=mac_address, limit=limit)`

**2. `get-arp-entry` command**
Options: `--id` (required)
Displays: All ArpEntrySummary fields in key-value panel format
Calls: `arp.get_arp_entry(client, id=id)`

Import `from nautobot_mcp.cms import arp` at top.
</action>

<acceptance_criteria>
- `cms_interfaces.py` contains `@app.command("list-arp-entries")`
- `cms_interfaces.py` contains `@app.command("get-arp-entry")`
- `cms_interfaces.py` contains `from nautobot_mcp.cms import arp`
- `uv run nautobot-mcp cms interfaces list-arp-entries --help` exits 0
- `uv run nautobot-mcp cms interfaces get-arp-entry --help` exits 0
</acceptance_criteria>

---

## Task 2: Add Routing Composite CLI Commands

<read_first>
- nautobot_mcp/cli/cms_routing.py (existing routing CLI commands — Typer app structure, --detail pattern)
- nautobot_mcp/cms/routing.py (get_device_bgp_summary, get_device_routing_table)
</read_first>

<action>
Add 2 composite CLI commands to `nautobot_mcp/cli/cms_routing.py`:

**1. `bgp-summary` command**
Options: `--device` (required), `--detail` (flag, default False)
Default table: Group Name | Type | Local AS | Neighbor Count
Detail table: Expands neighbors per group with Peer IP | Peer AS | State | AF Count | Policy Count
Calls: `routing.get_device_bgp_summary(client, device=device, detail=detail)`

**2. `routing-table` command**
Options: `--device` (required), `--detail` (flag, default False)
Default table: Destination | Preference | Routing Instance | Nexthop Count
Detail table: Expands next-hops per route with Next-hop IP | Interface | Metric | Preference
Calls: `routing.get_device_routing_table(client, device=device, detail=detail)`
</action>

<acceptance_criteria>
- `cms_routing.py` contains `@app.command("bgp-summary")`
- `cms_routing.py` contains `@app.command("routing-table")`
- `uv run nautobot-mcp cms routing bgp-summary --help` exits 0
- `uv run nautobot-mcp cms routing routing-table --help` exits 0
</acceptance_criteria>

---

## Task 3: Add Interface + Firewall Composite CLI Commands

<read_first>
- nautobot_mcp/cli/cms_interfaces.py (Typer app for interface commands)
- nautobot_mcp/cli/cms_firewalls.py (Typer app for firewall commands — existing structure)
- nautobot_mcp/cms/interfaces.py (get_interface_detail)
- nautobot_mcp/cms/firewalls.py (get_device_firewall_summary)
</read_first>

<action>
**In `cms_interfaces.py`:**

Add `detail` command:
Options: `--unit-id` (optional), `--device` (optional), `--interface` (optional), `--detail` (flag, default False)
Default: Panel with unit info + family names + counts (VRRP, ARP, filter, policer)
Detail: Expands families with filters, policers, VRRP groups with virtual IPs, ARP entries
Calls: `interfaces_cms.get_interface_detail(client, unit_id=unit_id, device=device, interface_name=interface, detail=detail)`

**In `cms_firewalls.py`:**

Add `firewall-summary` command:
Options: `--device` (required), `--detail` (flag, default False)
Default table: Filter Name | Family | Term Count + Policer Name | Bandwidth | Burst
Detail: Expands terms per filter with Term Name | Order | Enabled | Match Count | Action Count
Calls: `firewalls.get_device_firewall_summary(client, device=device, detail=detail)`
</action>

<acceptance_criteria>
- `cms_interfaces.py` contains `@app.command("detail")`
- `cms_firewalls.py` contains `@app.command("firewall-summary")`
- `uv run nautobot-mcp cms interfaces detail --help` exits 0
- `uv run nautobot-mcp cms firewalls firewall-summary --help` exits 0
</acceptance_criteria>

---

## Task 4: Create ARP Unit Tests

<read_first>
- tests/test_cms_routing.py (existing CMS test pattern — mock fixtures, model tests, CRUD function tests)
- tests/conftest.py (shared fixtures)
- nautobot_mcp/models/cms/arp.py (ArpEntrySummary model)
- nautobot_mcp/cms/arp.py (list_arp_entries, get_arp_entry)
</read_first>

<action>
Create `tests/test_cms_arp.py` with tests for:

**Model tests:**
- `test_arp_entry_from_nautobot()` — verify `ArpEntrySummary.from_nautobot()` extracts all fields correctly (interface_id, interface_name, device_name, ip_address, mac_address, hostname)
- `test_arp_entry_defaults()` — verify hostname defaults to empty string

**CRUD tests:**
- `test_list_arp_entries_by_device()` — mock `cms_list` call, verify device filter applied
- `test_list_arp_entries_by_interface()` — mock `cms_list` call, verify interface filter
- `test_list_arp_entries_by_mac()` — mock `cms_list` call, verify mac_address filter
- `test_list_arp_entries_combined_filters()` — verify device + interface filters combine
- `test_get_arp_entry()` — mock `cms_get` call, verify id lookup

Use `unittest.mock.patch` to mock `cms_list`, `cms_get`, `resolve_device_id`.
Follow patterns from `test_cms_routing.py`.
</action>

<acceptance_criteria>
- `test_cms_arp.py` contains `def test_arp_entry_from_nautobot`
- `test_cms_arp.py` contains `def test_list_arp_entries_by_device`
- `test_cms_arp.py` contains `def test_get_arp_entry`
- `uv run pytest tests/test_cms_arp.py -v` exits 0
- At least 7 test functions in the file
</acceptance_criteria>

---

## Task 5: Create Composite Summary Unit Tests

<read_first>
- tests/test_cms_routing.py (existing mock patterns for routing CRUD)
- tests/test_cms_firewalls.py (existing mock patterns for firewall CRUD)
- nautobot_mcp/cms/routing.py (get_device_bgp_summary, get_device_routing_table)
- nautobot_mcp/cms/interfaces.py (get_interface_detail)
- nautobot_mcp/cms/firewalls.py (get_device_firewall_summary)
</read_first>

<action>
Create `tests/test_cms_composites.py` with tests for all 4 composite functions:

**BGP Summary tests:**
- `test_bgp_summary_default()` — mock list_bgp_groups + list_bgp_neighbors, verify BGPSummaryResponse has correct groups, total_groups, total_neighbors
- `test_bgp_summary_detail()` — same + mock list_bgp_address_families + list_bgp_policy_associations, verify nested data inlined

**Routing Table tests:**
- `test_routing_table_default()` — mock list_static_routes, verify RoutingTableResponse has routes with nexthop count
- `test_routing_table_detail()` — same + mock list_static_route_nexthops + list_static_route_qualified_nexthops, verify nexthops inlined

**Interface Detail tests:**
- `test_interface_detail_by_id_default()` — mock get_interface_unit + list_interface_families + list_vrrp_groups + list_arp_entries, verify counts
- `test_interface_detail_by_id_detail()` — same + mock list_interface_family_filters + list_interface_family_policers, verify nested data

**Firewall Summary tests:**
- `test_firewall_summary_default()` — mock list_firewall_filters + list_firewall_policers, verify filter/policer counts
- `test_firewall_summary_detail()` — same + mock list_firewall_terms, verify terms inlined per filter

**Composite model tests:**
- `test_bgp_summary_response_model()` — verify BGPSummaryResponse fields
- `test_routing_table_response_model()` — verify RoutingTableResponse fields
- `test_interface_detail_response_model()` — verify InterfaceDetailResponse fields
- `test_firewall_summary_response_model()` — verify FirewallSummaryResponse fields

Use `unittest.mock.patch` to mock the individual CRUD functions called by composites.
</action>

<acceptance_criteria>
- `test_cms_composites.py` contains `def test_bgp_summary_default`
- `test_cms_composites.py` contains `def test_routing_table_default`
- `test_cms_composites.py` contains `def test_interface_detail_by_id_default`
- `test_cms_composites.py` contains `def test_firewall_summary_default`
- `uv run pytest tests/test_cms_composites.py -v` exits 0
- At least 12 test functions in the file
</acceptance_criteria>

---

## Task 6: Run Full Test Suite to Verify No Regressions

<read_first>
- tests/ (all test files — confirm no failures)
</read_first>

<action>
Run the full test suite and verify all tests pass:
```bash
uv run pytest tests/ -v --tb=short
```

If any failures: fix them before completing this plan.
</action>

<acceptance_criteria>
- `uv run pytest tests/ -v --tb=short` exits 0
- No test regressions (existing tests still pass)
- New tests (test_cms_arp.py + test_cms_composites.py) all pass
</acceptance_criteria>

---

<verification>
```bash
# CLI commands work
uv run nautobot-mcp cms interfaces list-arp-entries --help
uv run nautobot-mcp cms interfaces get-arp-entry --help
uv run nautobot-mcp cms interfaces detail --help
uv run nautobot-mcp cms routing bgp-summary --help
uv run nautobot-mcp cms routing routing-table --help
uv run nautobot-mcp cms firewalls firewall-summary --help

# All new tests pass
uv run pytest tests/test_cms_arp.py tests/test_cms_composites.py -v

# Full suite passes
uv run pytest tests/ -v --tb=short

# Count total Phase 12 tests
grep -c "def test_" tests/test_cms_arp.py tests/test_cms_composites.py
```
</verification>
