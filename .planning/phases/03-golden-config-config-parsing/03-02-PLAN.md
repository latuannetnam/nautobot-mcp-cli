---
phase: 03-golden-config-config-parsing
plan: 02
wave: 1
depends_on: []
subsystem: config-parser
tags: [parser, junos, json, abc, netutils]
autonomous: true
files_modified:
  - nautobot_mcp/parsers/__init__.py
  - nautobot_mcp/parsers/base.py
  - nautobot_mcp/parsers/junos.py
  - nautobot_mcp/models/parser.py
requirements: [PARSE-01, PARSE-02, PARSE-03, PARSE-04]
---

# Plan 03-02: JunOS Config Parser with ABC Architecture

<objective>
Build the config parser framework with a VendorParser ABC, JunOS JSON parser implementation, Nautobot-aligned pydantic output models, and auto-detection of platform variants (MX/EX/SRX).
</objective>

<must_haves>
- VendorParser ABC with registry pattern using network_os identifiers
- JunOS parser that processes `show configuration | display json` output
- Nautobot-aligned pydantic models: ParsedConfig, ParsedInterface, ParsedIPAddress, ParsedVLAN, ParsedRoutingInstance, ParsedProtocol, ParsedFirewallFilter, ParsedSystemSettings
- Auto-detect platform variant from config JSON tree
- ParsedConfig.warnings for unrecognized sections
- Single ParsedConfig container returned from parse
</must_haves>

<tasks>

<task id="1" title="Create parser pydantic models">
<read_first>
- nautobot_mcp/models/base.py
- nautobot_mcp/models/devices.py
- nautobot_mcp/models/interfaces.py
</read_first>
<action>
Create `nautobot_mcp/models/parser.py` with Nautobot-aligned pydantic models for parsed config data:

1. **ParsedInterface**:
```python
class ParsedInterface(BaseModel):
    name: str  # e.g., "ge-0/0/0"
    description: str = ""
    enabled: bool = True
    interface_type: str = ""  # "physical", "logical", "loopback", etc.
    units: list["ParsedInterfaceUnit"] = []
```

2. **ParsedInterfaceUnit**:
```python
class ParsedInterfaceUnit(BaseModel):
    unit: int = 0
    description: str = ""
    vlan_id: int | None = None
    ip_addresses: list["ParsedIPAddress"] = []
```

3. **ParsedIPAddress**:
```python
class ParsedIPAddress(BaseModel):
    address: str  # e.g., "10.0.0.1/30"
    family: str = "inet"  # "inet" or "inet6"
```

4. **ParsedVLAN**:
```python
class ParsedVLAN(BaseModel):
    name: str
    vlan_id: int
    description: str = ""
    l3_interface: str = ""  # associated IRB/VLAN interface
```

5. **ParsedRoutingInstance**:
```python
class ParsedRoutingInstance(BaseModel):
    name: str
    instance_type: str = ""  # "vrf", "virtual-router", etc.
    route_distinguisher: str = ""
    interfaces: list[str] = []  # interface names assigned to this instance
```

6. **ParsedProtocol**:
```python
class ParsedProtocol(BaseModel):
    protocol: str  # "bgp", "ospf", "isis", etc.
    local_as: int | None = None
    router_id: str = ""
    neighbors: list["ParsedProtocolNeighbor"] = []
    areas: list["ParsedOSPFArea"] = []
```

7. **ParsedProtocolNeighbor**:
```python
class ParsedProtocolNeighbor(BaseModel):
    address: str
    remote_as: int | None = None
    description: str = ""
    peer_group: str = ""
```

8. **ParsedOSPFArea**:
```python
class ParsedOSPFArea(BaseModel):
    area_id: str  # e.g., "0.0.0.0"
    interfaces: list[str] = []
```

9. **ParsedFirewallFilter**:
```python
class ParsedFirewallFilter(BaseModel):
    name: str
    family: str = "inet"
    term_count: int = 0
    terms: list[str] = []  # term names
```

10. **ParsedSystemSettings**:
```python
class ParsedSystemSettings(BaseModel):
    hostname: str = ""
    domain_name: str = ""
    name_servers: list[str] = []
    ntp_servers: list[str] = []
    syslog_hosts: list[str] = []
```

11. **ParsedConfig** — Top-level container:
```python
class ParsedConfig(BaseModel):
    hostname: str = ""
    platform: str = ""  # auto-detected: "MX", "EX", "SRX", or "junos"
    network_os: str = "juniper_junos"  # netutils-compatible identifier
    interfaces: list[ParsedInterface] = []
    ip_addresses: list[ParsedIPAddress] = []  # flattened from all interfaces
    vlans: list[ParsedVLAN] = []
    routing_instances: list[ParsedRoutingInstance] = []
    protocols: list[ParsedProtocol] = []
    firewall_filters: list[ParsedFirewallFilter] = []
    system: ParsedSystemSettings = ParsedSystemSettings()
    warnings: list[str] = []
```
</action>
<acceptance_criteria>
- nautobot_mcp/models/parser.py exists
- File contains `class ParsedInterface(BaseModel)`
- File contains `class ParsedInterfaceUnit(BaseModel)`
- File contains `class ParsedIPAddress(BaseModel)`
- File contains `class ParsedVLAN(BaseModel)`
- File contains `class ParsedRoutingInstance(BaseModel)`
- File contains `class ParsedProtocol(BaseModel)`
- File contains `class ParsedFirewallFilter(BaseModel)`
- File contains `class ParsedSystemSettings(BaseModel)`
- File contains `class ParsedConfig(BaseModel)`
- ParsedConfig contains `warnings: list[str]`
- ParsedConfig contains `platform: str`
- ParsedConfig contains `network_os: str`
</acceptance_criteria>
</task>

<task id="2" title="Create VendorParser ABC and parser registry">
<read_first>
- nautobot_mcp/models/parser.py
</read_first>
<action>
Create `nautobot_mcp/parsers/__init__.py` and `nautobot_mcp/parsers/base.py`.

**`nautobot_mcp/parsers/__init__.py`:**
```python
"""Config parser framework with vendor-extensible architecture."""
from nautobot_mcp.parsers.base import VendorParser, ParserRegistry
from nautobot_mcp.parsers.junos import JunosJsonParser

__all__ = ["VendorParser", "ParserRegistry", "JunosJsonParser"]
```

**`nautobot_mcp/parsers/base.py`:**
```python
from abc import ABC, abstractmethod
from nautobot_mcp.models.parser import ParsedConfig

class VendorParser(ABC):
    """Abstract base class for vendor config parsers.
    
    Subclasses must implement parse() and register via ParserRegistry.
    Identifier should match netutils network_os (e.g., "juniper_junos").
    """

    @property
    @abstractmethod
    def network_os(self) -> str:
        """netutils-compatible platform identifier."""
        ...

    @property
    @abstractmethod
    def vendor(self) -> str:
        """Vendor name (e.g., 'juniper')."""
        ...

    @abstractmethod
    def parse(self, config_data: dict | str) -> ParsedConfig:
        """Parse device config and return structured ParsedConfig.
        
        Args:
            config_data: JSON dict from 'show configuration | display json'
                         or raw text config string
        
        Returns:
            ParsedConfig with all extracted data and warnings for skipped sections
        """
        ...

    @abstractmethod
    def detect_platform(self, config_data: dict) -> str:
        """Auto-detect platform variant from config data.
        
        Returns platform hint string (e.g., "MX", "EX", "SRX").
        """
        ...


class ParserRegistry:
    """Registry for vendor parsers. Keyed by network_os identifier."""

    _parsers: dict[str, type[VendorParser]] = {}

    @classmethod
    def register(cls, parser_class: type[VendorParser]) -> type[VendorParser]:
        """Register a parser class. Can be used as decorator."""
        instance = parser_class()
        cls._parsers[instance.network_os] = parser_class
        return parser_class

    @classmethod
    def get(cls, network_os: str) -> VendorParser:
        """Get a parser instance by network_os identifier.
        
        Raises ValueError if no parser registered for that network_os.
        """
        if network_os not in cls._parsers:
            available = ", ".join(cls._parsers.keys()) or "none"
            raise ValueError(
                f"No parser registered for network_os='{network_os}'. "
                f"Available parsers: {available}"
            )
        return cls._parsers[network_os]()

    @classmethod
    def list_parsers(cls) -> list[str]:
        """Return list of registered network_os identifiers."""
        return list(cls._parsers.keys())
```
</action>
<acceptance_criteria>
- nautobot_mcp/parsers/__init__.py exists
- nautobot_mcp/parsers/base.py exists
- base.py contains `class VendorParser(ABC)`
- base.py contains `class ParserRegistry`
- base.py contains `def network_os(self) -> str`
- base.py contains `def parse(self, config_data`
- base.py contains `def detect_platform(self, config_data`
- base.py contains `@classmethod` and `def register(`
- base.py contains `@classmethod` and `def get(`
- base.py contains `from abc import ABC, abstractmethod`
</acceptance_criteria>
</task>

<task id="3" title="Create JunOS JSON parser implementation">
<read_first>
- nautobot_mcp/parsers/base.py
- nautobot_mcp/models/parser.py
</read_first>
<action>
Create `nautobot_mcp/parsers/junos.py` with a complete JunOS JSON parser that extracts all network infrastructure data.

```python
import json
import logging
from nautobot_mcp.parsers.base import VendorParser, ParserRegistry
from nautobot_mcp.models.parser import (
    ParsedConfig, ParsedInterface, ParsedInterfaceUnit, ParsedIPAddress,
    ParsedVLAN, ParsedRoutingInstance, ParsedProtocol, ParsedProtocolNeighbor,
    ParsedOSPFArea, ParsedFirewallFilter, ParsedSystemSettings,
)

logger = logging.getLogger(__name__)

KNOWN_SECTIONS = {
    "interfaces", "vlans", "routing-instances", "protocols",
    "firewall", "system", "routing-options", "policy-options",
    "class-of-service", "security", "ethernet-switching",
    "chassis", "snmp", "services", "applications",
    "groups", "apply-groups",
}

@ParserRegistry.register
class JunosJsonParser(VendorParser):
    """Parser for JunOS JSON config output (show configuration | display json)."""

    @property
    def network_os(self) -> str:
        return "juniper_junos"

    @property
    def vendor(self) -> str:
        return "juniper"

    def detect_platform(self, config_data: dict) -> str:
        config = config_data.get("configuration", config_data)
        if "security" in config:
            return "SRX"
        if any("ethernet-switching" in str(iface) for iface in config.get("interfaces", {}).get("interface", [])):
            return "EX"
        return "MX"

    def parse(self, config_data: dict | str) -> ParsedConfig:
        if isinstance(config_data, str):
            config_data = json.loads(config_data)
        
        config = config_data.get("configuration", config_data)
        warnings = []
        
        # Detect platform
        platform = self.detect_platform(config_data)
        
        # Parse each section
        system = self._parse_system(config.get("system", {}))
        interfaces = self._parse_interfaces(config.get("interfaces", {}))
        ip_addresses = self._flatten_ip_addresses(interfaces)
        vlans = self._parse_vlans(config.get("vlans", {}))
        routing_instances = self._parse_routing_instances(config.get("routing-instances", {}))
        protocols = self._parse_protocols(config.get("protocols", {}))
        firewall_filters = self._parse_firewall(config.get("firewall", {}))
        
        # Record warnings for unrecognized sections
        for key in config:
            if key.startswith("@"):
                continue
            if key not in KNOWN_SECTIONS:
                warnings.append(f"Skipped unrecognized config section: {key}")
        
        return ParsedConfig(
            hostname=system.hostname,
            platform=platform,
            network_os=self.network_os,
            interfaces=interfaces,
            ip_addresses=ip_addresses,
            vlans=vlans,
            routing_instances=routing_instances,
            protocols=protocols,
            firewall_filters=firewall_filters,
            system=system,
            warnings=warnings,
        )
```

Implement these private methods:

- `_parse_system(self, system_data: dict) -> ParsedSystemSettings` — Extract hostname, domain-name, name-server, ntp, syslog from system block
- `_parse_interfaces(self, iface_data: dict) -> list[ParsedInterface]` — Extract interface name, description, unit/family/address hierarchy
- `_flatten_ip_addresses(self, interfaces: list[ParsedInterface]) -> list[ParsedIPAddress]` — Collect all IPs from all interface units
- `_parse_vlans(self, vlan_data: dict) -> list[ParsedVLAN]` — Extract vlan name, vlan-id
- `_parse_routing_instances(self, ri_data: dict) -> list[ParsedRoutingInstance]` — Extract instance name, type, RD, interfaces
- `_parse_protocols(self, proto_data: dict) -> list[ParsedProtocol]` — Extract BGP (neighbors, AS), OSPF (areas, interfaces)
- `_parse_firewall(self, fw_data: dict) -> list[ParsedFirewallFilter]` — Extract filter names, family, term names

Each method handles missing keys gracefully with `.get()` and default values.
JunOS JSON array pattern: all lists are arrays even if single element.
Field names use hyphens: `vlan-id`, `routing-instances`, `name-server`.
</action>
<acceptance_criteria>
- nautobot_mcp/parsers/junos.py exists
- File contains `class JunosJsonParser(VendorParser)`
- File contains `@ParserRegistry.register`
- File contains `def detect_platform(self, config_data`
- File contains `def parse(self, config_data`
- File contains `return "SRX"` (platform detection)
- File contains `return "EX"` (platform detection)
- File contains `return "MX"` (platform detection)
- File contains `def _parse_system(`
- File contains `def _parse_interfaces(`
- File contains `def _parse_vlans(`
- File contains `def _parse_protocols(`
- File contains `def _parse_firewall(`
- File contains `def _parse_routing_instances(`
- File contains `KNOWN_SECTIONS`
- File contains `warnings.append(` for unknown sections
</acceptance_criteria>
</task>

</tasks>
