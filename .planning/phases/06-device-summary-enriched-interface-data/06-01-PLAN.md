---
phase: 6
plan: "06-01"
title: "Device Summary Composite Tool"
wave: 1
depends_on: []
files_modified:
  - nautobot_mcp/models/device.py
  - nautobot_mcp/devices.py
  - nautobot_mcp/server.py
  - nautobot_mcp/cli/devices.py
  - tests/test_server.py
requirements:
  - SUMM-01
  - SUMM-02
  - SUMM-03
autonomous: true
---

# Plan 06-01: Device Summary Composite Tool

## Objective

Create a `nautobot_device_summary` MCP tool that answers "tell me everything about device X" in one call. Composes existing functions (`get_device`, `list_interfaces`, `get_device_ips`, `list_vlans`) into a single aggregated response with counts and link state stats.

## must_haves

1. Single MCP tool call returns device info + interfaces + IPs + VLANs
2. Response includes counts: interface_count, ip_count, vlan_count
3. Response includes link state stats: enabled_count, disabled_count
4. CLI `devices summary DEVICE` shows compact overview by default
5. CLI `devices summary DEVICE --detail` shows full interface/IP breakdown

## Tasks

<task id="06-01-01">
<title>Add DeviceSummaryResponse model to models/device.py</title>
<read_first>
- nautobot_mcp/models/device.py (DeviceSummary model)
- nautobot_mcp/models/interface.py (InterfaceSummary model)
- nautobot_mcp/models/ipam.py (DeviceIPEntry, VLANSummary)
</read_first>
<action>
Add a new Pydantic model `DeviceSummaryResponse` at end of `nautobot_mcp/models/device.py`:

```python
from nautobot_mcp.models.interface import InterfaceSummary
from nautobot_mcp.models.ipam import DeviceIPEntry, VLANSummary


class DeviceSummaryResponse(BaseModel):
    """Complete device overview: info + interfaces + IPs + VLANs + counts."""

    device: DeviceSummary = Field(description="Core device info")
    interfaces: list[InterfaceSummary] = Field(
        default_factory=list,
        description="All interfaces on this device",
    )
    interface_ips: list[DeviceIPEntry] = Field(
        default_factory=list,
        description="IPs assigned to interfaces via M2M",
    )
    vlans: list[VLANSummary] = Field(
        default_factory=list,
        description="VLANs on device interfaces",
    )
    interface_count: int = Field(default=0, description="Total interface count")
    ip_count: int = Field(default=0, description="Total IP count")
    vlan_count: int = Field(default=0, description="Total VLAN count")
    enabled_count: int = Field(default=0, description="Enabled interfaces")
    disabled_count: int = Field(default=0, description="Disabled interfaces")
```
</action>
<acceptance_criteria>
- `nautobot_mcp/models/device.py` contains `class DeviceSummaryResponse(BaseModel):`
- Model has fields: device, interfaces, interface_ips, vlans, interface_count, ip_count, vlan_count, enabled_count, disabled_count
- `from nautobot_mcp.models.device import DeviceSummaryResponse` succeeds
</acceptance_criteria>
</task>

<task id="06-01-02">
<title>Add get_device_summary core function to devices.py</title>
<read_first>
- nautobot_mcp/devices.py (get_device function)
- nautobot_mcp/interfaces.py (list_interfaces function)
- nautobot_mcp/ipam.py (get_device_ips, list_vlans functions)
- nautobot_mcp/models/device.py (DeviceSummaryResponse after 06-01-01)
</read_first>
<action>
Add new function `get_device_summary` at end of `nautobot_mcp/devices.py`:

```python
from nautobot_mcp import interfaces as iface_mod
from nautobot_mcp import ipam as ipam_mod
from nautobot_mcp.models.device import DeviceSummaryResponse


def get_device_summary(
    client: NautobotClient,
    name: str,
) -> DeviceSummaryResponse:
    """Get complete device overview: info + interfaces + IPs + VLANs + counts.

    Composes get_device, list_interfaces, get_device_ips, and list_vlans
    into a single response. Uses existing functions — no duplicate API logic.

    Args:
        client: NautobotClient instance.
        name: Device hostname.

    Returns:
        DeviceSummaryResponse with all device data and counts.
    """
    # Step 1: Get device info
    device = get_device(client, name=name)

    # Step 2: Get interfaces
    iface_result = iface_mod.list_interfaces(client, device_name=name, limit=0)
    iface_list = iface_result.results

    # Step 3: Get IPs via M2M
    ip_result = ipam_mod.get_device_ips(client, device_name=name)

    # Step 4: Get VLANs via interfaces
    vlan_result = ipam_mod.list_vlans(client, device=name, limit=0)

    # Step 5: Compute counts
    enabled_count = sum(1 for i in iface_list if i.enabled)
    disabled_count = len(iface_list) - enabled_count

    return DeviceSummaryResponse(
        device=device,
        interfaces=iface_list,
        interface_ips=ip_result.interface_ips,
        vlans=vlan_result.results,
        interface_count=len(iface_list),
        ip_count=ip_result.total_ips,
        vlan_count=vlan_result.count,
        enabled_count=enabled_count,
        disabled_count=disabled_count,
    )
```

Add imports at top:
```python
from nautobot_mcp import interfaces as iface_mod
from nautobot_mcp import ipam as ipam_mod
from nautobot_mcp.models.device import DeviceSummaryResponse
```

Note: Move imports inside function or at module level (avoid circular — interfaces/ipam don't import devices, so module-level is OK).
</action>
<acceptance_criteria>
- `nautobot_mcp/devices.py` contains `def get_device_summary(`
- Function takes `client: NautobotClient` and `name: str`
- Function returns `DeviceSummaryResponse`
- Function calls `get_device`, `list_interfaces`, `get_device_ips`, `list_vlans`
- Function computes `enabled_count` and `disabled_count`
</acceptance_criteria>
</task>

<task id="06-01-03">
<title>Add nautobot_device_summary MCP tool to server.py</title>
<read_first>
- nautobot_mcp/server.py (existing device tool patterns lines 62-196)
- nautobot_mcp/devices.py (get_device_summary after 06-01-02)
</read_first>
<action>
Add new MCP tool in `nautobot_mcp/server.py` after `nautobot_delete_device` (after line ~196):

```python
@mcp.tool(name="nautobot_device_summary")
def nautobot_device_summary(
    device_name: str,
) -> dict:
    """Get complete device overview in one call: info, interfaces, IPs, VLANs, and counts.

    Answers "tell me everything about device X" — no need to call multiple tools.
    Includes interface count, IP count, VLAN count, and link state statistics.

    Args:
        device_name: Device hostname (exact match).

    Returns:
        Dict with device (info), interfaces (list), interface_ips (list),
        vlans (list), and counts (interface_count, ip_count, vlan_count,
        enabled_count, disabled_count).
    """
    try:
        client = get_client()
        result = devices.get_device_summary(client, name=device_name)
        return result.model_dump()
    except Exception as e:
        handle_error(e)
```
</action>
<acceptance_criteria>
- `nautobot_mcp/server.py` contains `@mcp.tool(name="nautobot_device_summary")`
- Function parameter is `device_name: str` (required)
- Function calls `devices.get_device_summary(client, name=device_name)`
- Function returns `result.model_dump()`
</acceptance_criteria>
</task>

<task id="06-01-04">
<title>Add CLI summary command to cli/devices.py</title>
<read_first>
- nautobot_mcp/cli/devices.py (existing device commands)
- nautobot_mcp/cli/formatters.py (output patterns)
</read_first>
<action>
Add new CLI command in `nautobot_mcp/cli/devices.py` after `devices_delete`:

```python
@devices_app.command("summary")
def devices_summary(
    ctx: typer.Context,
    name: str = typer.Argument(help="Device name"),
    detail: bool = typer.Option(False, "--detail", help="Show full interface/IP breakdown"),
) -> None:
    """Get complete device overview with counts and stats."""
    try:
        client = get_client_from_ctx(ctx)
        result = devices.get_device_summary(client, name=name)
        data = result.model_dump()

        if ctx.obj.get("json", False):
            import json
            typer.echo(json.dumps(data, indent=2))
            return

        # Always show compact overview
        d = data["device"]
        typer.echo(f"Device: {d['name']} ({d['status']})")
        typer.echo(f"  Type: {d['device_type']['name']} | Location: {d['location']['name']}")
        if d.get("platform"):
            typer.echo(f"  Platform: {d['platform']}")
        if d.get("primary_ip"):
            typer.echo(f"  Primary IP: {d['primary_ip']}")
        typer.echo(f"\n  Interfaces: {data['interface_count']} (↑{data['enabled_count']} ↓{data['disabled_count']})")
        typer.echo(f"  IP Addresses: {data['ip_count']}")
        typer.echo(f"  VLANs: {data['vlan_count']}")

        if detail:
            typer.echo(f"\n  --- Interfaces ---")
            for iface in data["interfaces"]:
                status = "↑" if iface["enabled"] else "↓"
                desc = f" — {iface['description']}" if iface.get("description") else ""
                typer.echo(f"  {status} {iface['name']} ({iface['type']}){desc}")
            if data["interface_ips"]:
                typer.echo(f"\n  --- IP Assignments ---")
                for ip in data["interface_ips"]:
                    typer.echo(f"  {ip['interface_name']}: {ip['address']} ({ip['status']})")
            if data["vlans"]:
                typer.echo(f"\n  --- VLANs ---")
                for vlan in data["vlans"]:
                    typer.echo(f"  VLAN {vlan['vid']}: {vlan['name']} ({vlan['status']})")

    except Exception as e:
        handle_cli_error(e)
```
</action>
<acceptance_criteria>
- `nautobot_mcp/cli/devices.py` contains `@devices_app.command("summary")`
- Command takes positional `name` argument and optional `--detail` flag
- Running `uv run nautobot-mcp devices summary --help` shows the command
- Default output shows compact overview with counts
- `--detail` output shows full interface/IP/VLAN breakdown
</acceptance_criteria>
</task>

<task id="06-01-05">
<title>Add unit tests for device_summary</title>
<read_first>
- tests/test_server.py (existing test patterns with @patch and MagicMock)
- tests/conftest.py (mock_device_record fixture)
</read_first>
<action>
Add test class to `tests/test_server.py`:

```python
class TestDeviceSummary:
    """Test nautobot_device_summary MCP tool."""

    @patch("nautobot_mcp.server.get_client")
    def test_device_summary_returns_dict(self, mock_get_client):
        """device_summary returns aggregated dict with counts."""
        mock_client = MagicMock()

        # Mock device
        mock_device = MagicMock()
        mock_device.id = "dev-uuid"
        mock_device.name = "test-device"
        mock_device.status.display = "Active"
        mock_device.device_type = MagicMock(id="dt-1", name="MX204", display="MX204")
        mock_device.location = MagicMock(id="loc-1", name="DC1", display="DC1")
        mock_device.tenant = None
        mock_device.role = None
        mock_device.platform = None
        mock_device.serial = None
        mock_device.primary_ip = None
        mock_client.api.dcim.devices.get.return_value = mock_device

        # Mock interfaces
        mock_iface = MagicMock()
        mock_iface.id = "iface-uuid-1"
        mock_iface.name = "ae0.0"
        mock_iface.type.display = "Virtual"
        mock_iface.device = MagicMock(id="dev-uuid", name="test-device", display="test-device")
        mock_iface.enabled = True
        mock_iface.description = None
        mock_iface.mac_address = None
        mock_iface.mtu = None
        mock_iface.ip_addresses = []
        mock_client.api.dcim.interfaces.filter.return_value = [mock_iface]

        # Mock M2M (no IPs)
        mock_client.api.ipam.ip_address_to_interface.filter.return_value = []

        # Mock VLANs (none)
        mock_iface.untagged_vlan = None
        mock_iface.tagged_vlans = []

        mock_get_client.return_value = mock_client

        from nautobot_mcp.server import nautobot_device_summary
        result = nautobot_device_summary(device_name="test-device")

        assert isinstance(result, dict)
        assert result["device"]["name"] == "test-device"
        assert result["interface_count"] == 1
        assert result["ip_count"] == 0
        assert result["vlan_count"] == 0
        assert result["enabled_count"] == 1
        assert result["disabled_count"] == 0

    @patch("nautobot_mcp.server.get_client")
    def test_device_summary_tool_registered(self, mock_get_client):
        """nautobot_device_summary should be registered as an MCP tool."""
        import asyncio
        from nautobot_mcp.server import mcp
        tools = asyncio.run(mcp.list_tools())
        names = [t.name for t in tools]
        assert "nautobot_device_summary" in names
```
</action>
<acceptance_criteria>
- `tests/test_server.py` contains `class TestDeviceSummary:`
- `tests/test_server.py` contains `def test_device_summary_returns_dict(`
- `tests/test_server.py` contains `def test_device_summary_tool_registered(`
- Running `uv run pytest tests/test_server.py -k "test_device_summary"` passes
</acceptance_criteria>
</task>

## Verification

```bash
# All existing tests still pass
uv run pytest tests/ -v

# New tool is registered
uv run python -c "
import asyncio
from nautobot_mcp.server import mcp
tools = asyncio.run(mcp.list_tools())
names = [t.name for t in tools]
assert 'nautobot_device_summary' in names, f'Tool not found. Tools: {names}'
print(f'✓ nautobot_device_summary tool registered ({len(tools)} total)')
"

# CLI command shows help
uv run nautobot-mcp devices summary --help
```
