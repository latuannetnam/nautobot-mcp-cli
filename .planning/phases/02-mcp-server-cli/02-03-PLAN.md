---
phase: 02-mcp-server-cli
plan: 03
wave: 2
depends_on: [02-01, 02-02]
subsystem: testing
tags: [tests, pytest, mcp, cli]
autonomous: true
files_modified:
  - tests/test_server.py
  - tests/test_cli.py
  - nautobot_mcp/__init__.py
requirements: [MCP-01, MCP-02, MCP-03, MCP-04, CLI-01, CLI-02, CLI-03, CLI-04]
---

# Plan 02-03: MCP Server and CLI Tests + Package Exports

<objective>
Create comprehensive test suites for the MCP server tools and CLI commands, verifying tool schemas, output formats, error handling, and exit codes. Update package exports.
</objective>

<must_haves>
- MCP server tests verifying tool registration, output shapes, and error handling
- CLI tests verifying command structure, table/JSON output, and exit codes
- Package __init__.py updated with new exports
- All existing tests still pass
</must_haves>

<tasks>

<task id="1" title="Create MCP server tests">
<read_first>
- nautobot_mcp/server.py
- tests/conftest.py
- tests/test_models.py
</read_first>
<action>
Create `tests/test_server.py` with:

1. Import the mcp server instance and tool functions
2. Test tool registration: verify `mcp` has tools registered with `nautobot_` prefix
3. Test tool output shape: mock `NautobotClient`, call device tool, verify returns dict with expected keys
4. Test error handling: mock client raising `NautobotNotFoundError`, verify `ToolError` is raised
5. Test client factory: verify `get_client()` returns `NautobotClient` instance (mock settings)

Minimum tests:
- `test_mcp_server_has_tools` — verify `mcp._tool_manager` contains tools
- `test_tool_names_have_nautobot_prefix` — all registered tool names start with `nautobot_`
- `test_list_devices_tool_returns_dict` — mock client, verify dict output
- `test_get_device_tool_not_found_raises_tool_error` — verify ToolError on NautobotNotFoundError
- `test_client_factory_creates_client` — verify get_client returns NautobotClient

Use monkeypatch to mock NautobotSettings and NautobotClient.__init__.
</action>
<acceptance_criteria>
- tests/test_server.py exists
- File contains `def test_mcp_server_has_tools`
- File contains `def test_tool_names_have_nautobot_prefix`
- File contains `def test_list_devices_tool_returns_dict`
- File contains `def test_get_device_tool_not_found_raises_tool_error`
- File contains `from fastmcp.exceptions import ToolError`
- `pytest tests/test_server.py` exits 0
</acceptance_criteria>
</task>

<task id="2" title="Create CLI tests">
<read_first>
- nautobot_mcp/cli/app.py
- nautobot_mcp/cli/devices.py
- nautobot_mcp/cli/formatters.py
</read_first>
<action>
Create `tests/test_cli.py` with:

1. Use `typer.testing.CliRunner` to invoke commands
2. Test formatters: verify `format_table` produces tabulate output, `format_json` produces valid JSON
3. Test CLI structure: verify `nautobot-mcp devices list --help` works
4. Test JSON output: mock client, invoke with `--json`, verify JSON shape `{"count": N, "results": [...]}`
5. Test exit codes: mock client raising exceptions, verify correct exit codes

Minimum tests:
- `test_format_table_produces_simple_table` — verify tabulate output
- `test_format_json_produces_valid_json` — verify JSON output
- `test_devices_list_help` — CliRunner invoke, verify exit code 0 and "Usage" in output
- `test_devices_list_json_output` — mock client, verify JSON shape
- `test_connection_error_exit_code_2` — verify exit code 2
- `test_not_found_error_exit_code_3` — verify exit code 3

Use `from typer.testing import CliRunner` and `runner.invoke(app, ["devices", "list", "--help"])`.
</action>
<acceptance_criteria>
- tests/test_cli.py exists
- File contains `from typer.testing import CliRunner`
- File contains `def test_format_table_produces_simple_table`
- File contains `def test_format_json_produces_valid_json`
- File contains `def test_devices_list_help`
- File contains `def test_connection_error_exit_code_2`
- `pytest tests/test_cli.py` exits 0
</acceptance_criteria>
</task>

<task id="3" title="Update package exports">
<read_first>
- nautobot_mcp/__init__.py
</read_first>
<action>
Update `nautobot_mcp/__init__.py` to add server module reference in a comment (not importing mcp server at package level to avoid requiring fastmcp for library-only usage).

Add comment section:
```python
# MCP Server: nautobot_mcp.server (import separately)
# CLI: nautobot_mcp.cli.app (registered as entry point)
```

Verify all existing exports still work:
```python
pytest tests/ -v
```
</action>
<acceptance_criteria>
- nautobot_mcp/__init__.py contains `# MCP Server`
- nautobot_mcp/__init__.py contains `# CLI`
- `pytest tests/` exits 0 (all tests pass including new ones)
- Total test count > 35 (31 existing + new server + cli tests)
</acceptance_criteria>
</task>

</tasks>
