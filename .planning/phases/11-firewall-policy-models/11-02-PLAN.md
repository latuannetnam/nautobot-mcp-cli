---
phase: 11
plan: 11-02
title: "Firewall Domain CRUD Functions & MCP Tools"
wave: 2
depends_on: [11-01]
files_modified:
  - nautobot_mcp/cms/firewalls.py [NEW]
  - nautobot_mcp/server.py
autonomous: true
requirements_addressed: [FW-01, FW-02, FW-03, FW-04, FW-05, FW-06, FW-07]
---

<objective>
Create firewall domain CRUD functions in `nautobot_mcp/cms/firewalls.py` and register corresponding MCP tools in `server.py`. Covers 7 endpoints: firewall filters (full CRUD), firewall policers (full CRUD), and 5 sub-models (read-only list/get).
</objective>

<must_haves>
- `cms/firewalls.py` module with all CRUD functions for 7 firewall endpoints
- 6 top-level models get full CRUD (FirewallFilter, FirewallPolicer) — actually 2 in this domain
- 5 sub-models get read-only (Term, MatchCondition, MatchConditionToPrefixList, FilterAction, PolicerAction)
- Shallow list (counts only) / Rich get (inlined children) hybrid pattern
- MCP tool registrations in `server.py` under `# CMS FIREWALL TOOLS` section
- Policer name-only inlining in action responses
</must_haves>

---

## Task 1: Create Firewall CRUD Module

<read_first>
- nautobot_mcp/cms/routing.py (established CRUD pattern — list/get/create/update/delete functions, device-scoping, inlining)
- nautobot_mcp/cms/interfaces.py (hybrid inlining pattern — shallow list with counts, rich get)
- nautobot_mcp/cms/client.py (cms_list, cms_get, cms_create, cms_update, cms_delete, resolve_device_id)
- nautobot_mcp/models/cms/firewalls.py (Pydantic models from Plan 11-01)
- .planning/phases/11-firewall-policy-models/11-CONTEXT.md (decisions on CRUD scope and inlining)
</read_first>

<action>
Create `nautobot_mcp/cms/firewalls.py` with these functions:

### Full CRUD — FirewallFilter (device-scoped)

```python
def list_firewall_filters(client, device, family=None, limit=0) -> ListResponse[FirewallFilterSummary]:
    """List firewall filters for a device. Populates term_count per filter."""
    # resolve_device_id, cms_list with device filter
    # Batch-fetch all terms for device, count per filter_id, populate filter.term_count
    
def get_firewall_filter(client, id) -> FirewallFilterSummary:
    """Get filter with inlined term summaries (name, order, match_count, action_count)."""
    # cms_get the filter
    # Fetch terms for this filter
    # For each term, count match_conditions and actions
    # Populate filter.term_count and a terms list
    
def create_firewall_filter(client, device, name, family="inet", **kwargs) -> FirewallFilterSummary:
    """Create a firewall filter. device-scoped via resolve_device_id."""
    
def update_firewall_filter(client, id, **updates) -> FirewallFilterSummary:
    
def delete_firewall_filter(client, id) -> dict:
```

### Full CRUD — FirewallPolicer (device-scoped)

```python
def list_firewall_policers(client, device, limit=0) -> ListResponse[FirewallPolicerSummary]:
    """List policers for a device. Populates action_count per policer."""
    
def get_firewall_policer(client, id) -> FirewallPolicerSummary:
    """Get policer with inlined actions."""
    
def create_firewall_policer(client, device, name, **kwargs) -> FirewallPolicerSummary:
    
def update_firewall_policer(client, id, **updates) -> FirewallPolicerSummary:
    
def delete_firewall_policer(client, id) -> dict:
```

### Read-only — FirewallTerm, MatchCondition, FilterAction, PolicerAction, MatchConditionToPrefixList

```python
def list_firewall_terms(client, filter_id=None, limit=0) -> ListResponse[FirewallTermSummary]:
    """List terms, optionally filtered by parent filter. Populates match_count, action_count."""
    
def get_firewall_term(client, id) -> FirewallTermSummary:
    """Get term with inlined match conditions and actions. Actions include policer_name."""
    
def list_firewall_match_conditions(client, term_id=None, limit=0) -> ListResponse[FirewallMatchConditionSummary]:
    
def get_firewall_match_condition(client, id) -> FirewallMatchConditionSummary:

def list_firewall_filter_actions(client, term_id=None, limit=0) -> ListResponse[FirewallFilterActionSummary]:
    
def get_firewall_filter_action(client, id) -> FirewallFilterActionSummary:

def list_firewall_policer_actions(client, policer_id=None, limit=0) -> ListResponse[FirewallPolicerActionSummary]:
    
def get_firewall_policer_action(client, id) -> FirewallPolicerActionSummary:

def list_firewall_match_condition_prefix_lists(client, match_condition_id=None, limit=0) -> ListResponse[FirewallMatchConditionToPrefixListSummary]:
    
def get_firewall_match_condition_prefix_list(client, id) -> FirewallMatchConditionToPrefixListSummary:
```

Follow the exact error-handling pattern from `routing.py`: wrap in try/except, call `client._handle_api_error(e, ...)`, re-raise.

Endpoint names (pynautobot underscore format):
- `juniper_firewall_filters`
- `juniper_firewall_terms`
- `juniper_firewall_match_conditions`
- `juniper_firewall_actions`
- `juniper_firewall_policers`
- `juniper_firewall_policer_actions`
- `juniper_firewall_match_condition_prefix_lists`
</action>

<acceptance_criteria>
- `firewalls.py` contains `def list_firewall_filters(`
- `firewalls.py` contains `def get_firewall_filter(`
- `firewalls.py` contains `def create_firewall_filter(`
- `firewalls.py` contains `def update_firewall_filter(`
- `firewalls.py` contains `def delete_firewall_filter(`
- `firewalls.py` contains `def list_firewall_policers(`
- `firewalls.py` contains `def list_firewall_terms(`
- `firewalls.py` contains `def get_firewall_term(`
- `firewalls.py` contains `from nautobot_mcp.cms.client import`
- 20 total function definitions (2×5 full CRUD + 5×2 read-only = 20)
- `python -c "from nautobot_mcp.cms.firewalls import list_firewall_filters, create_firewall_filter"` exits 0
</acceptance_criteria>

---

## Task 2: Register Firewall MCP Tools in server.py

<read_first>
- nautobot_mcp/server.py (lines 1800-2425 for CMS tool pattern — `@mcp.tool(name="nautobot_cms_*")`, try/except/handle_error)
- nautobot_mcp/cms/firewalls.py (the CRUD functions from Task 1)
</read_first>

<action>
Add a `# CMS FIREWALL TOOLS` section in `server.py` after the CMS INTERFACE TOOLS section (before the `# Server entry point` section).

Add import at top: `from nautobot_mcp.cms import firewalls as cms_firewalls`

Register these MCP tools following the exact pattern of CMS interface tools (lines 1821-2417):

**Full CRUD tools (FirewallFilter — 5 tools):**
- `nautobot_cms_list_firewall_filters(device: str, family: Optional[str] = None, limit: int = 50)`
- `nautobot_cms_get_firewall_filter(id: str)`
- `nautobot_cms_create_firewall_filter(device: str, name: str, family: str = "inet")`
- `nautobot_cms_update_firewall_filter(id: str, name: Optional[str] = None, description: Optional[str] = None)`
- `nautobot_cms_delete_firewall_filter(id: str)`

**Full CRUD tools (FirewallPolicer — 5 tools):**
- `nautobot_cms_list_firewall_policers(device: str, limit: int = 50)`
- `nautobot_cms_get_firewall_policer(id: str)`
- `nautobot_cms_create_firewall_policer(device: str, name: str, bandwidth_limit: Optional[int] = None, ...)`
- `nautobot_cms_update_firewall_policer(id: str, ...)`
- `nautobot_cms_delete_firewall_policer(id: str)`

**Read-only tools (10 tools — list/get for 5 sub-models):**
- `nautobot_cms_list_firewall_terms(filter_id: Optional[str] = None, limit: int = 50)` / `nautobot_cms_get_firewall_term(id: str)`
- `nautobot_cms_list_firewall_match_conditions(term_id: Optional[str] = None, limit: int = 50)` / `nautobot_cms_get_firewall_match_condition(id: str)`
- `nautobot_cms_list_firewall_filter_actions(term_id: Optional[str] = None, limit: int = 50)` / `nautobot_cms_get_firewall_filter_action(id: str)`
- `nautobot_cms_list_firewall_policer_actions(policer_id: Optional[str] = None, limit: int = 50)` / `nautobot_cms_get_firewall_policer_action(id: str)`
- `nautobot_cms_list_firewall_match_condition_prefix_lists(match_condition_id: Optional[str] = None, limit: int = 50)` / `nautobot_cms_get_firewall_match_condition_prefix_list(id: str)`

Total: **20 MCP tools** for firewall domain.

Each tool follows: `@mcp.tool(name="...")`, try/except/handle_error, `result.model_dump()`.
</action>

<acceptance_criteria>
- `server.py` contains `from nautobot_mcp.cms import firewalls as cms_firewalls`
- `server.py` contains `# CMS FIREWALL TOOLS` or `# ===========================================================================` header
- `server.py` contains `@mcp.tool(name="nautobot_cms_list_firewall_filters")`
- `server.py` contains `@mcp.tool(name="nautobot_cms_create_firewall_filter")`
- `server.py` contains `@mcp.tool(name="nautobot_cms_list_firewall_policers")`
- `server.py` contains `@mcp.tool(name="nautobot_cms_get_firewall_term")`
- `server.py` contains `@mcp.tool(name="nautobot_cms_list_firewall_policer_actions")`
- 20 new `@mcp.tool` decorators for firewall domain
- `python -c "from nautobot_mcp.server import mcp; print('server loads')"` exits 0
</acceptance_criteria>

---

<verification>
```bash
# Module imports
python -c "from nautobot_mcp.cms.firewalls import list_firewall_filters, get_firewall_filter, create_firewall_filter, update_firewall_filter, delete_firewall_filter, list_firewall_policers, get_firewall_policer, create_firewall_policer, list_firewall_terms, get_firewall_term; print('All firewall CRUD functions importable')"

# Server loads with new tools
python -c "from nautobot_mcp.server import mcp; print('Server loads successfully')"

# Count firewall tools
grep -c "nautobot_cms_.*firewall" nautobot_mcp/server.py
# Should be >= 20
```
</verification>
