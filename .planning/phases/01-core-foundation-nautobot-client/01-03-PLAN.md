---
phase: 01-core-foundation-nautobot-client
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [nautobot_mcp/models/ipam.py, nautobot_mcp/models/organization.py, nautobot_mcp/models/circuit.py, nautobot_mcp/ipam.py, nautobot_mcp/organization.py, nautobot_mcp/circuits.py]
autonomous: true
requirements: [IPAM-01, IPAM-02, IPAM-03, IPAM-04, IPAM-05, IPAM-06, ORG-01, ORG-02, ORG-03, ORG-04, CIR-01, CIR-02, CIR-03]

must_haves:
  truths:
    - "User can list and create prefixes with filtering"
    - "User can list and create IP addresses"
    - "User can list and create VLANs"
    - "User can list and manage tenants"
    - "User can list and manage locations"
    - "User can list and manage circuits"
  artifacts:
    - path: "nautobot_mcp/models/ipam.py"
      provides: "IPAM pydantic models"
      contains: "class PrefixSummary"
    - path: "nautobot_mcp/ipam.py"
      provides: "IPAM CRUD operations"
      contains: "def list_prefixes"
    - path: "nautobot_mcp/organization.py"
      provides: "Organization operations"
      contains: "def list_tenants"
    - path: "nautobot_mcp/circuits.py"
      provides: "Circuit operations"
      contains: "def list_circuits"
  key_links:
    - from: "nautobot_mcp/ipam.py"
      to: "nautobot_mcp/client.py"
      via: "uses NautobotClient"
      pattern: "NautobotClient"
---

<objective>
Implement IPAM (Prefix, IP Address, VLAN), Organization (Tenant, Location), and Circuit CRUD operations. These follow the same patterns established in Plan 02.

Purpose: Complete all core Nautobot data model coverage for v1.
Output: IPAM, Organization, and Circuit domain modules with pydantic models.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-core-foundation-nautobot-client/01-CONTEXT.md
@.planning/phases/01-core-foundation-nautobot-client/01-RESEARCH.md
@.planning/phases/01-core-foundation-nautobot-client/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create IPAM models and operations</name>
  <files>nautobot_mcp/models/ipam.py, nautobot_mcp/ipam.py</files>
  <read_first>nautobot_mcp/devices.py, nautobot_mcp/models/device.py, nautobot_mcp/models/base.py, nautobot_mcp/client.py</read_first>
  <action>
1. Create `nautobot_mcp/models/ipam.py`:
   - `PrefixSummary(BaseModel)`: id, prefix (str, e.g. "10.0.0.0/24"), status, namespace: RelatedObject, location: Optional[RelatedObject], tenant: Optional[RelatedObject], vlan: Optional[RelatedObject], type: str (Container/Network/Pool), description
   - `IPAddressSummary(BaseModel)`: id, address (str), status, namespace: RelatedObject, tenant: Optional[RelatedObject], dns_name: Optional[str], type: str
   - `VLANSummary(BaseModel)`: id, vid (int), name, status, location: Optional[RelatedObject], tenant: Optional[RelatedObject], vlan_group: Optional[RelatedObject]
   - All have `@classmethod from_nautobot(cls, record)`

2. Create `nautobot_mcp/ipam.py`:
   - `list_prefixes(client, location=None, tenant=None, namespace=None, vrf=None, q=None, limit=0, **extra) -> ListResponse[PrefixSummary]`
   - `create_prefix(client, prefix: str, namespace: str = "Global", status: str = "Active", **kwargs) -> PrefixSummary`
     - Note: Nautobot v2 requires Namespace for prefix uniqueness
   - `list_ip_addresses(client, device=None, interface=None, prefix=None, q=None, limit=0, **extra) -> ListResponse[IPAddressSummary]`
   - `create_ip_address(client, address: str, namespace: str = "Global", status: str = "Active", **kwargs) -> IPAddressSummary`
   - `list_vlans(client, location=None, tenant=None, vlan_group=None, limit=0, **extra) -> ListResponse[VLANSummary]`
   - `create_vlan(client, vid: int, name: str, status: str = "Active", **kwargs) -> VLANSummary`
  </action>
  <verify>python -c "from nautobot_mcp.ipam import list_prefixes, list_ip_addresses, list_vlans; print('OK')"</verify>
  <acceptance_criteria>
    - models/ipam.py contains "class PrefixSummary"
    - models/ipam.py contains "class IPAddressSummary"
    - models/ipam.py contains "class VLANSummary"
    - models/ipam.py contains "namespace"
    - ipam.py contains "def list_prefixes"
    - ipam.py contains "def create_prefix"
    - ipam.py contains "def list_ip_addresses"
    - ipam.py contains "def create_ip_address"
    - ipam.py contains "def list_vlans"
    - ipam.py contains "def create_vlan"
  </acceptance_criteria>
  <done>IPAM operations work with Nautobot v2 Namespace support</done>
</task>

<task type="auto">
  <name>Task 2: Create Organization and Circuit modules</name>
  <files>nautobot_mcp/models/organization.py, nautobot_mcp/models/circuit.py, nautobot_mcp/organization.py, nautobot_mcp/circuits.py</files>
  <read_first>nautobot_mcp/devices.py, nautobot_mcp/models/base.py</read_first>
  <action>
1. Create `nautobot_mcp/models/organization.py`:
   - `TenantSummary(BaseModel)`: id, name, tenant_group: Optional[RelatedObject], description
   - `LocationSummary(BaseModel)`: id, name, location_type: RelatedObject, parent: Optional[RelatedObject], tenant: Optional[RelatedObject], status
   - Both have `from_nautobot()` classmethod

2. Create `nautobot_mcp/organization.py`:
   - `list_tenants(client, q=None, limit=0, **extra) -> ListResponse[TenantSummary]`
   - `get_tenant(client, name=None, id=None) -> TenantSummary`
   - `create_tenant(client, name: str, **kwargs) -> TenantSummary`
   - `update_tenant(client, id: str, **updates) -> TenantSummary`
   - `list_locations(client, location_type=None, parent=None, tenant=None, q=None, limit=0, **extra) -> ListResponse[LocationSummary]`
   - `get_location(client, name=None, id=None) -> LocationSummary`
   - `create_location(client, name: str, location_type: str, status: str = "Active", **kwargs) -> LocationSummary`
   - `update_location(client, id: str, **updates) -> LocationSummary`

3. Create `nautobot_mcp/models/circuit.py`:
   - `CircuitSummary(BaseModel)`: id, cid (circuit ID string), provider: RelatedObject, circuit_type: RelatedObject, status, tenant: Optional[RelatedObject], description
   - `from_nautobot()` classmethod

4. Create `nautobot_mcp/circuits.py`:
   - `list_circuits(client, provider=None, circuit_type=None, location=None, q=None, limit=0, **extra) -> ListResponse[CircuitSummary]`
   - `get_circuit(client, cid=None, id=None) -> CircuitSummary`
   - `create_circuit(client, cid: str, provider: str, circuit_type: str, **kwargs) -> CircuitSummary`
   - `update_circuit(client, id: str, **updates) -> CircuitSummary`
  </action>
  <verify>python -c "from nautobot_mcp.organization import list_tenants, list_locations; from nautobot_mcp.circuits import list_circuits; print('OK')"</verify>
  <acceptance_criteria>
    - models/organization.py contains "class TenantSummary"
    - models/organization.py contains "class LocationSummary"
    - models/circuit.py contains "class CircuitSummary"
    - organization.py contains "def list_tenants"
    - organization.py contains "def list_locations"
    - organization.py contains "def create_tenant"
    - organization.py contains "def create_location"
    - circuits.py contains "def list_circuits"
    - circuits.py contains "def get_circuit"
    - circuits.py contains "def create_circuit"
  </acceptance_criteria>
  <done>Organization and Circuit modules complete the core data model coverage</done>
</task>

</tasks>

<verification>
- [ ] All IPAM imports work: `from nautobot_mcp.ipam import list_prefixes, list_ip_addresses, list_vlans`
- [ ] All Org imports work: `from nautobot_mcp.organization import list_tenants, list_locations`
- [ ] All Circuit imports work: `from nautobot_mcp.circuits import list_circuits, get_circuit`
- [ ] All models have `from_nautobot()` classmethods
</verification>

<success_criteria>
- All tasks completed
- IPAM module handles Nautobot v2 Namespaces
- Organization module handles Location with LocationType hierarchy
- Circuit module handles provider + type relationships
- All follow the same pattern as Device module (Plan 02)
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-foundation-nautobot-client/01-03-SUMMARY.md`
</output>
