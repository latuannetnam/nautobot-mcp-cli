---
phase: 01-core-foundation-nautobot-client
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03"]
files_modified: [tests/test_config.py, tests/test_exceptions.py, tests/test_models.py, tests/conftest.py, nautobot_mcp/__init__.py]
autonomous: true
requirements: [CORE-01, CORE-02, CORE-03, CORE-04]

must_haves:
  truths:
    - "Unit tests verify config loading from env vars and YAML"
    - "Unit tests verify exception hierarchy and to_dict()"
    - "Unit tests verify pydantic model creation and from_nautobot()"
    - "All imports work from package root"
  artifacts:
    - path: "tests/test_config.py"
      provides: "Config system tests"
      contains: "def test_"
      min_lines: 30
    - path: "tests/test_exceptions.py"
      provides: "Exception tests"
      contains: "def test_"
    - path: "tests/test_models.py"
      provides: "Model tests"
      contains: "def test_"
    - path: "tests/conftest.py"
      provides: "Shared test fixtures"
      contains: "pytest.fixture"
  key_links:
    - from: "tests/test_config.py"
      to: "nautobot_mcp/config.py"
      via: "tests config classes"
      pattern: "NautobotSettings|NautobotProfile"
---

<objective>
Create unit tests for the core foundation: config system, exceptions, and pydantic models. Also update package __init__.py to export all public API.

Purpose: Verify the foundation works correctly before building MCP/CLI layers on top of it. Tests use mocked pynautobot to avoid needing a real Nautobot server.
Output: Comprehensive test suite for Phase 1 foundation.
</objective>

<execution_context>
@.agent/get-shit-done/workflows/execute-plan.md
@.agent/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-core-foundation-nautobot-client/01-CONTEXT.md
@.planning/phases/01-core-foundation-nautobot-client/01-01-SUMMARY.md
@.planning/phases/01-core-foundation-nautobot-client/01-02-SUMMARY.md
@.planning/phases/01-core-foundation-nautobot-client/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test fixtures and config tests</name>
  <files>tests/__init__.py, tests/conftest.py, tests/test_config.py</files>
  <read_first>nautobot_mcp/config.py, nautobot_mcp/client.py</read_first>
  <action>
1. Create `tests/__init__.py` (empty)

2. Create `tests/conftest.py`:
   - Fixture `mock_nautobot_profile()` → returns NautobotProfile(url="https://test.nautobot.local", token="test-token-123")
   - Fixture `mock_settings()` → returns NautobotSettings with test profiles
   - Fixture `mock_device_record()` → returns a mock object mimicking pynautobot Record with .id, .name, .location, .device_type attributes
   - Fixture `mock_interface_record()` → similar for interfaces

3. Create `tests/test_config.py`:
   - `test_profile_creation()` — NautobotProfile with url/token/verify_ssl
   - `test_settings_default_profile()` — default profile is "default"
   - `test_settings_env_override(monkeypatch)` — set NAUTOBOT_URL and NAUTOBOT_TOKEN env vars, verify they override
   - `test_settings_get_active_profile()` — returns correct profile
   - `test_settings_multiple_profiles()` — create settings with production and staging profiles
   - `test_profile_verify_ssl_default_true()` — SSL verification is True by default
  </action>
  <verify>python -m pytest tests/test_config.py -v</verify>
  <acceptance_criteria>
    - tests/conftest.py contains "mock_nautobot_profile"
    - tests/conftest.py contains "pytest.fixture"
    - tests/test_config.py contains "def test_profile_creation"
    - tests/test_config.py contains "def test_settings_env_override"
    - tests/test_config.py contains "def test_settings_multiple_profiles"
    - "python -m pytest tests/test_config.py" exits 0
  </acceptance_criteria>
  <done>Config tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Create exception and model tests, update package exports</name>
  <files>tests/test_exceptions.py, tests/test_models.py, nautobot_mcp/__init__.py</files>
  <read_first>nautobot_mcp/exceptions.py, nautobot_mcp/models/base.py, nautobot_mcp/models/device.py, nautobot_mcp/__init__.py</read_first>
  <action>
1. Create `tests/test_exceptions.py`:
   - `test_base_exception_to_dict()` — verify to_dict returns {"error": ..., "hint": ..., "code": ...}
   - `test_connection_error_code()` — code is "CONNECTION_ERROR"
   - `test_not_found_error_hint()` — hint contains actionable guidance
   - `test_validation_error()` — NautobotValidationError works
   - `test_exception_inheritance()` — all inherit from NautobotMCPError

2. Create `tests/test_models.py`:
   - `test_related_object_creation()` — RelatedObject(id="...", name="...")
   - `test_list_response()` — ListResponse(count=2, results=[...])
   - `test_device_summary_from_dict()` — DeviceSummary with all fields
   - `test_interface_summary()` — InterfaceSummary creation
   - `test_prefix_summary()` — PrefixSummary with namespace
   - `test_related_object_optional_display()` — display field is optional

3. Update `nautobot_mcp/__init__.py`:
   - Export key classes: NautobotClient, NautobotSettings, NautobotProfile
   - Export exceptions: NautobotMCPError, NautobotConnectionError, NautobotNotFoundError, etc.
   - Export domain functions: list_devices, get_device, list_interfaces, list_prefixes, etc.
   - Use `__all__` list for explicit public API
  </action>
  <verify>python -m pytest tests/ -v</verify>
  <acceptance_criteria>
    - tests/test_exceptions.py contains "def test_base_exception_to_dict"
    - tests/test_exceptions.py contains "def test_connection_error_code"
    - tests/test_models.py contains "def test_related_object_creation"
    - tests/test_models.py contains "def test_device_summary"
    - tests/test_models.py contains "def test_list_response"
    - nautobot_mcp/__init__.py contains "__all__"
    - nautobot_mcp/__init__.py contains "NautobotClient"
    - "python -m pytest tests/ -v" exits 0
  </acceptance_criteria>
  <done>All tests pass and package exports all public API</done>
</task>

</tasks>

<verification>
- [ ] `python -m pytest tests/ -v` — all tests pass
- [ ] `python -c "from nautobot_mcp import NautobotClient, NautobotSettings, list_devices"` works
- [ ] Test coverage includes config, exceptions, and models
</verification>

<success_criteria>
- All unit tests pass
- Package exports all public API through __init__.py
- Test fixtures enable easy testing for future development
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-foundation-nautobot-client/01-04-SUMMARY.md`
</output>
